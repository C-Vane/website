# Public site URL with no trailing slash. Used for canonical links, Open Graph, JSON-LD, /sitemap.xml, and /robots.txt. Set to your production domain in live environments.
NEXT_PUBLIC_BASE_URL=http://localhost:4444
NEXT_PUBLIC_ALGOLIA_API_KEY=
NEXT_PUBLIC_ALGOLIA_INDEX_NAME=